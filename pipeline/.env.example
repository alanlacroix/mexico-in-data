# Copy this file to  pipeline/.env  and paste your free tokens.
# .env is gitignored — it never leaves your machine. In CI these are GitHub secrets.
# Both are free, one-time, and unlock 5 live pulse sources (peso, rates, inflation, GDP).

# INEGI — register at https://www.inegi.org.mx/servicios/api_indicadores.html
INEGI_TOKEN=

# Banxico SIE — one captcha at https://www.banxico.org.mx/SieAPIRest/service/v1/token
BANXICO_TOKEN=
