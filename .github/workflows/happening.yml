name: happening
# The only workflow that publishes the homepage brief. It gets several chances to
# run in each editorial window, but the committed publication receipt makes each
# window idempotent. Every workflow that writes to main shares the concurrency
# group below, so a data refresh cannot race this commit again.

on:
  schedule:
    # One edition a day (Alan, 2026-08-03, the $2/month budget call): the morning
    # window only. Two non-top-of-hour chances in both possible UTC hours; the gate
    # resolves Eastern time and skips duplicates after the publication receipt.
    # The evening cron came off — the wire and the number tiles keep refreshing on
    # their own deterministic schedule, and a breaking evening story can still be
    # published by manual dispatch.
    - cron: '7,37 13,14 * * *'  # 9 AM Eastern, EST or EDT
    # Catch-up. GitHub's scheduler is best-effort and dropped all four morning
    # occurrences on 2026-08-03, so the site sat on the previous day's edition. The
    # publication receipt makes these no-ops once the window has published.
    - cron: '11,41 15,16,17 * * *' # late morning Eastern, catch-up only
    - cron: '26,56 23,0 * * *'     # late evening Eastern, catch-up only
  workflow_dispatch:
    inputs:
      slot:
        description: Edition to publish
        required: true
        default: auto
        type: choice
        options:
          - auto
          - morning
          - afternoon
      reason:
        description: Why this run was requested
        required: false
        type: string
      force:
        description: Republish even if the repository receipt is current
        required: true
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: mexico-brief-production-write
  cancel-in-progress: false

jobs:
  happening:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: npm

      - name: Decide whether this edition is still due
        id: timing
        env:
          REQUESTED_SLOT: ${{ github.event.inputs.slot || 'auto' }}
          SCHEDULE: ${{ github.event.schedule }}
          FORCE_PUBLICATION: ${{ github.event.inputs.force || 'false' }}
        run: node pipeline/editorial-gate.mjs

      - name: Install exact dependencies
        if: steps.timing.outputs.run == 'true'
        run: npm ci

      - name: Check the latest trusted headlines
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        continue-on-error: true
        run: node build-news.js

      - name: Refresh the news ledger
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        continue-on-error: true
        run: node collect-news.js

      - name: Build the event log
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node build-happening.js

      - name: Build the homepage brief
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node build-brief.js

      - name: Translate the new edition for /es/
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        continue-on-error: true   # /es/ shows English for anything untranslated; never blocks publication
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node translate-es.mjs

      - name: Build the topic areas
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node build-areas.js

      - name: Build the companies tracker
        if: steps.timing.outputs.run == 'true'
        working-directory: pipeline
        continue-on-error: true
        run: node build-companies.js

      - name: Validate generated editorial claims
        if: steps.timing.outputs.run == 'true'
        run: |
          if node pipeline/assert-data.js && node pipeline/test/homepage-feed-contract.test.mjs; then
            exit 0
          fi

          echo "::warning::Generated copy failed validation; rebuilding from the last approved event log."
          git show HEAD:data/happening.json > data/happening.json
          git show HEAD:data/brief.json > data/brief.json
          git show HEAD:data/areas.json > data/areas.json
          git show HEAD:data/companies.json > data/companies.json
          (
            cd pipeline
            node build-happening.js
            node build-brief.js
            node build-areas.js
            node build-companies.js || true
          )
          node pipeline/assert-data.js
          node pipeline/test/homepage-feed-contract.test.mjs

      - name: Write this edition's publication receipt
        if: steps.timing.outputs.run == 'true'
        env:
          PUBLICATION_SLOT: ${{ steps.timing.outputs.slot }}
          PUBLICATION_DATE: ${{ steps.timing.outputs.editorial_date }}
          PUBLICATION_ID: ${{ github.run_id }}-${{ github.run_attempt }}
        run: node pipeline/write-publication-status.mjs

      - name: Build and validate the exact production artifact
        if: steps.timing.outputs.run == 'true'
        run: npm run release

      - name: Commit and push the edition once
        if: steps.timing.outputs.run == 'true'
        env:
          PUBLICATION_SLOT: ${{ steps.timing.outputs.slot }}
          PUBLICATION_DATE: ${{ steps.timing.outputs.editorial_date }}
        run: |
          git config user.name  "mexico-data-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/news.json data/news/ data/happening.json data/brief.json data/areas.json data/companies.json data/publication-status.json data/llm-spend.json data/es/
          git commit -m "publication: ${PUBLICATION_DATE} ${PUBLICATION_SLOT} edition"
          # A single push attempt used to lose the whole publication to a non-fast-forward
          # whenever anything else landed on main mid-run (2026-08-02 22:14: the edition
          # published, then the run died here). The machine owns these paths, so rebasing
          # onto whatever arrived and trying again is always right.
          for attempt in 1 2 3 4 5; do
            if git push origin HEAD:main; then
              echo "pushed on attempt $attempt"
              exit 0
            fi
            echo "push rejected, rebasing onto origin/main (attempt $attempt)"
            git pull --rebase --autostash origin main || exit 1
            sleep 5
          done
          echo "could not push the edition after 5 attempts"
          exit 1

      - name: Require the exact edition to be live
        if: steps.timing.outputs.run == 'true'
        id: live
        continue-on-error: true
        env:
          PUBLICATION_SLOT: ${{ steps.timing.outputs.slot }}
          PUBLICATION_DATE: ${{ steps.timing.outputs.editorial_date }}
          PUBLICATION_ID: ${{ github.run_id }}-${{ github.run_attempt }}
        run: node pipeline/verify-production.mjs

      - name: Retrigger deployment once if production is stale
        if: steps.timing.outputs.run == 'true' && steps.live.outcome == 'failure'
        env:
          PUBLICATION_SLOT: ${{ steps.timing.outputs.slot }}
          PUBLICATION_DATE: ${{ steps.timing.outputs.editorial_date }}
          PUBLICATION_ID: ${{ github.run_id }}-${{ github.run_attempt }}
          DEPLOY_ATTEMPT: '2'
        run: |
          node pipeline/write-publication-status.mjs
          git add data/publication-status.json
          git commit -m "publication: retry ${PUBLICATION_DATE} ${PUBLICATION_SLOT} deployment"
          git push origin HEAD:main
          node pipeline/verify-production.mjs
