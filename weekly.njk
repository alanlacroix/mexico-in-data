---
layout: base.njk
permalink: /weekly.html
title: "Subscribe · The Mexico Brief"
description: "A free email on Mexico every Monday: what changed in the economy, politics, business, crime, and the US relationship, from original sources, every figure linked so you can check the work."
---
{% raw %}
<style>
  :root{--ink-3:#4d4d4d}
  body{font-size:var(--fs-body)}

  .lp{max-width:640px;padding:40px 0 8px}
  @media(min-width:760px){.lp{padding:64px 0 12px}}
  .lp .eyebrow{font-family:var(--mono);font-size:var(--fs-label);letter-spacing:.06em;text-transform:uppercase;color:var(--green-d);font-weight:700}
  .lp h1{font-family:var(--sans);font-size:var(--fs-display);line-height:1.04;letter-spacing:-.025em;margin:14px 0 0;color:var(--ink);max-width:18ch}
  .lp .thesis{font-size:var(--fs-body);color:var(--ink-2);margin:18px 0 0;line-height:1.5;max-width:52ch}

  .signup{margin:28px 0 0;max-width:480px}
  .signup form{display:flex;gap:10px;flex-wrap:wrap}
  .signup input[type=email]{flex:1;min-width:220px;font-family:var(--sans);font-size:var(--fs-small);padding:14px 15px;border:1.5px solid var(--line);border-radius:10px;background:#fff;color:var(--ink)}
  .signup input[type=email]:focus{outline:none;border-color:var(--green)}
  .signup button{font-family:var(--mono);font-size:var(--fs-label);letter-spacing:.05em;text-transform:uppercase;font-weight:700;color:#fff;background:var(--green);border:none;border-radius:10px;padding:14px 24px;cursor:pointer}
  .signup button:hover{background:var(--green-d)}
  .signup .fine{font-size:var(--fs-small);color:var(--ink-2);margin-top:13px;line-height:1.55}

  .who{display:flex;gap:12px;align-items:center;border-top:1px solid var(--line);margin-top:38px;padding-top:20px;max-width:60ch}
  .who .avatar{width:40px;height:40px;border-radius:50%;background:#e7f3ec;color:var(--green-d);display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-weight:700;font-size:var(--fs-small);flex:0 0 auto}
  .who p{font-size:var(--fs-small);color:var(--ink-2);margin:0;line-height:1.5}
  .who a{color:var(--green);font-weight:500}
</style>

<div class="wrap">
  <section class="lp">
    <span class="eyebrow">The email · free</span>
    <h1>What's changing in Mexico.</h1>
    <p class="thesis">Every Monday, I look at what changed in Mexico's economy, politics, business, crime, and relationship with the United States. The numbers come from original sources. The links are there too, in case you want to check my work.</p>

    <!-- Buttondown embed. Replace `mexicobrief` with the real username once the account exists. -->
    <div class="signup">
      <form action="https://buttondown.email/api/emails/embed-subscribe/mexicobrief" method="post" target="popupwindow" onsubmit="window.open('https://buttondown.email/mexicobrief','popupwindow')">
        <input type="email" name="email" placeholder="you@email.com" aria-label="Email address" required />
        <input type="hidden" value="1" name="embed" />
        <button type="submit">Subscribe</button>
      </form>
      <div class="fine">Free. One email, Monday mornings.</div>
    </div>
  </section>
</div>
{% endraw %}
